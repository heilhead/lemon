#pragma once

#include <folly/experimental/coro/AsyncGenerator.h>

namespace lemon::scheduler {
    class Scheduler {
    public:
//        static folly::coro::AsyncGenerator<int> generateInts(int begin, int end);
    };
}
