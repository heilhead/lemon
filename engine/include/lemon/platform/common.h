#pragma once

namespace lemon::platform {
    enum class SystemPath {
        UserDocuments,
        UserAppData,
        UserSavedGames,
        UserScreenshots,
        SharedAppData,
        Fonts,
        OSRoot,
    };
}
